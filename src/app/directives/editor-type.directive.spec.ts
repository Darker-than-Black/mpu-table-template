import { EditorTypeDirective } from './editor-type.directive';

describe('EditorTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorTypeDirective();
    expect(directive).toBeTruthy();
  });
});
